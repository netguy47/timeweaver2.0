# Contributing to TimeWeaver Predictive Core

- Fork → Branch → Commit → PR against `main`.
- Include tests for new functionality (pytest).
- Update docs for user-facing changes.
- Keep public copy free of internal method names.
